version: '3.8'
services:
  db:
    image: postgres:15
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: raciel
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: nestdb
    ports:
      - "5499:5432"

volumes:
  db_data:
